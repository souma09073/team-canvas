// ============================================================
// 調整する数値は全部ここ。
// ゲームバランスを変えたいときは、このファイルだけ触ればいい。
// 変えたら Run し直すだけで反映される。
//
// ┌─ よく使う早見表 ────────────────────────────────┐
// │ 全体を速く / 遅く       → SPEED_SCALE(下にある。これ1つ) │
// │ コースを短くしたい      → Regions.pde の targetSec を下げる │
// │ 食べ物を減らしたい      → Regions.pde の .food(...) の      │
// │                           間隔(秒)を大きくする            │
// │ メーターの動きを穏やかに→ Regions.pde の .food(...) の      │
// │                           上昇と減少を同じ倍率で下げる     │
// │ 全体を易しく            → HYPER_GRACE_SEC を上げる         │
// │ 止まる時間を短く        → COLLAPSE_STOP_SEC / ROCK_STOP_SEC │
// └────────────────────────────────────────────────┘
//
// 【注意1】エリアごとに変わる数値は Regions.pde にある。
//   食べ物の間隔・血糖の上昇と減少・密集地帯の有無は、エリアが進むほど
//   難しくなるよう表で組んであるので、そちらで調整する。
//
// 【注意2】STABLE_MAX を変えるときは、HYPER_THRESHOLD と HYPER_RESET_BELOW も
//   同じ数字にする(3ヶ所セット)。
//
// 【注意3】高血糖で死ぬ条件:
//   (100 - STABLE_MAX) ÷ (減少 × DRAIN_HIGH_MULT) が HYPER_GRACE_SEC を超えると
//   「振り切ったら確定死」になる。北海道(減少14)なら 35÷21 = 1.67秒 対 3秒。
// ============================================================

// ---- 画面 ----
final int SCREEN_W = 1280;   // 設計上の画面サイズ。実画面へは拡大縮小して表示する
final int SCREEN_H = 720;

// 画面いっぱいで起動するか。
//   true  … どのPCでも必ず収まる。提出・発表はこちら(終了は ESC キー)
//   false … ウィンドウで起動する。コードを直しながら試すときはこちら
//
// ウィンドウ表示だと、Windowsの表示倍率(125%/150%)やマルチモニタの環境で
// 画面からはみ出すことがある。指定した大きさより実際の表示が大きくなるため。
// フルスクリーンならその心配がない。
final boolean FULLSCREEN = true;

// ---- 動作確認用 ----
// タイトルの説明画面で 1〜4 を押すと、そのステージから始められる。
// 北海道だけ直したいのに毎回沖縄から走るのは時間の無駄なので入れてある。
//
// 途中から始めた回はハイスコアに記録されない(手前を走っていないぶんタイムが短いため)。
// 提出版やお披露目のときに隠したければ false にする。
final boolean DEBUG_STAGE_SELECT = true;

// ---- 見た目 ----
// イラストを使うか。false にすると、画像があっても全部が球と箱の表示に戻る。
// 何かおかしくなったら、まずここを false にして切り分ける。
final boolean USE_IMAGES = true;

// 主人公の絵の高さ(ワールド単位)。球で描いていたときの体格に合わせてある。
// 大きすぎ・小さすぎを感じたらここを変える。
final float PLAYER_IMG_HEIGHT = 3.4;

// 絵を貼った板をカメラ側へ倒す角度(度)。
// カメラは少し見下ろしているので、0のままだと「地面に立てたシール」に見えることがある。
// その場合は 10〜20 くらいを試す。0 で真っ直ぐ立つ。
final float BILLBOARD_TILT = 0;

// ---- 血糖 ----
// 【食べ物の上昇幅と自然減少は Regions.pde にある】エリアごとに違うため。
// 沖縄 +16/-8 → 北海道 +28/-14。必要ペースはどのエリアも 2.0秒に1個で同じだが、
// 1回食べ損ねたときの落ち方・1回食べすぎたときの跳ね方が大きくなっていく。
// 高血糖のときは減少が何倍になるか。
// 1.5 だと沖縄(減少8)で 100→65 に 2.9秒かかり、猶予3秒に対して余裕が0.1秒しかない。
// 振り切った瞬間にほぼ確定で倒れる状態だったので 1.8 に上げた(余裕0.57秒)。
// 【副作用】全エリアで高血糖から抜けやすくなる。戻すならこの数字を 1.5 に。
final float DRAIN_HIGH_MULT    = 1.8;
final float STABLE_MIN         = 35;    // 安定域(緑)の下限 = ゾーンが溜まる範囲の下限
final float STABLE_MAX         = 65;    // 安定域(緑)の上限
final float HYPER_THRESHOLD    = 65;    // これを超えると高血糖カウントダウン開始
final float HYPER_GRACE_SEC    = 3;     // 猶予(秒)
final float HYPER_RESET_BELOW  = 65;    // ここまで下げれば解除
final float DANGER_ZONE        = 15;    // これ未満で低血糖の警告(青)

// ---- 倒れたときの減速 ----
// 血糖が振り切れても、以前のように即ゲームオーバーにはしない。
// 一定時間だけ大きく減速し、治療を受けて走り続ける。
//
// 【この変更の理由】
// ・やり直しを無くすと、失敗しても最後まで通しで遊べる(人に見せるときに効く)
// ・タイムアタックなので「遅くなる」ことが十分な罰になる
// ・先生から「休みをちょうだい」と言われており、即死の緊張を下げたい
//
// 【止まる。減速ではない】
// 減速だと「3秒間40%だから実質1.8秒ロス」という分かりにくい計算になる。
// 完全に止めれば、止まった秒数がそのまま失う時間になる。
// 倒れて治療を受けているのだから、走っていないほうが自然でもある。
//
// 止まっている間は、前進も血糖も当たり判定も全部止まる。進むのは経過タイムだけ。
final float COLLAPSE_STOP_SEC = 2.0;   // 血糖で倒れたときに止まる秒数
final float GLUCOSE_RESET     = 50;    // 治療を受けたとき / 港を出るときの血糖値

// ---- 岩(障害物) ----
// 血糖とは無関係の、純粋な障害物。ぶつかると止まる。
// 倒れたときより短いのは、こちらは「見て避けそこねた」だけで、
// 血糖の管理に失敗したわけではないため。
final float ROCK_STOP_SEC = 0.8;     // 岩にぶつかったときに止まる秒数
final float ROCK_GAP_MIN  = 320;     // 岩の間隔・最小。大きいほどまばら
final float ROCK_GAP_MAX  = 620;     // 岩の間隔・最大

// ---- フェリーとカウントダウン ----
// エリアの区切りはフェリーで移動する(企画書の設定)。
// 3回あるうち、真ん中の1回だけ止まって途中経過を見せる。
// 全部止めるとテンポが悪く、全部流すと休む間が無いため。
final float FERRY_SEC     = 3.0;   // 自動で流れるフェリーの演出時間
final float COUNTDOWN_SEC = 3.0;   // 走り出す前のカウントダウン
// どちらも経過タイムには含めない。走っていない時間が記録に混ざらないようにする。

// ---- マンジャロ(注射) ----
// マンジャロは【1エリアにつき1回だけ】打てる。
//
// 【この仕組みにした理由】
// 現実のマンジャロは週1回の持続性の薬で、血糖が上がった瞬間に打つ薬ではない
// (それはインスリンの役割)。旧版は7秒ごとに何度でも打てたので、
// 「週1回の薬」という事実と矛盾していた。
// 1エリア = 1週間 とみなし、打てる回数をエリアごとに1回へ制限した。
// ただし【いつ打つかは自由】。現実でも「今週の分をいつ打つか」は自分で決める。
//
// 効果は「食べ物が無効になる」。食べ物を消すのではなく、
// そこにあるまま素通りする(食欲が失せた状態)。血糖には直接影響しない。
// 効いている秒数。
// 5.0 だと一度打てば区間まるごと素通りできてしまい、「打っておけば安全」になっていた。
// 目の前の2〜3個を無効にするだけの長さに縮めて、どこで打つかを選ばせる。
final float SHOT_EFFECT_SEC = 1.2;
// 効いている間は「血糖が上がらない」。食べても吸収されない(これが薬の作用そのもの)。
// 下降は止めず 80% に緩めるだけ。完全に止めると低血糖のリスクが消えてしまい、
// マンジャロの危険性が表現できなくなるため。
final float SHOT_DRAIN_MULT = 0.8;   // 効果中の血糖降下の倍率(1.0=通常どおり / 0=止まる)
final float LOCK_DURATION   = 7;     // 女性に接触したとき撃てなくなる秒数

// ---- ゾーン ----
final float ZONE_CHARGE_PER_SEC = 8;     // 緑の帯に居る間の蓄積(%/秒)
final float ZONE_DECAY_PER_SEC  = 0;     // 帯を外れた時の減少(%/秒)。0=貯金は消えない
final float FOOD_POP_SEC        = 0.5;   // 食べた演出の長さ(秒)
// ---- エナジードリンク ----
// 1エリアに1本だけ落ちている。拾うとストックされ、好きなときに使える。
//
// 【速く走れる。ただし血糖が跳ね上がる】
// エナジードリンクは大量の糖分そのものなので、飲めば血糖が一気に上がる。
// 加速だけの都合のいいアイテムにすると、拾ったら即使うのが常に正解になり、
// 判断が消える。代償を付けることで「どこで使うか」がゲームになる。
//
// 血糖50で使うと85まで跳ねて高血糖の秒読みが始まる。20の超過なので、
// そこから食べずに走れば戻せる。危ないが、狙えば使える範囲。
// 血糖10まで落ちているときに使えば45に戻り、低血糖を切り抜けつつ加速できる。
// つまり【追い詰められているときほど強い】アイテムになっている。
//
// 45 で試したところ「使う=ほぼ倒れる」になり、誰も使わないアイテムになった。
// 罰が重すぎると選択肢が消えるので 35 に下げてある。
final float ENERGY_DURATION   = 1.5;   // 加速している時間(秒)
final float ENERGY_SPEED_MULT = 1.8;   // 加速中の速度倍率
final float ENERGY_GAIN       = 35;    // 飲んだ瞬間の血糖上昇。食べ物1個(16〜28)より大きい

// ---- 速度とコース ----
// エリアごとの速度は Regions.pde の buildRegions() にある。
// エリアが進むほど速くなり、フェリーに着くたび開始速度へ戻る。
// 各エリアの長さは「目標秒数 ÷ 速度」から自動計算されるので、ここには書かない。

// ★ 全体の速さを変えるならここだけ触る。1.0 が基準。
//   上げるとコースも自動で長くなるので、各エリアのタイムは25秒のまま変わらない。
//   「全体的にもっと速く」と思ったら、この数字を 1.2 → 1.4 → 1.6 と振ってみること。
//   レーン移動の速さも一緒に上がるので、操作が置いていかれることはない。
final float SPEED_SCALE = 1.1;

final float LANE_WIDTH        = 4;
final float LANE_CHANGE_BASE  = 24;    // レーン移動の速さの基準値(units/秒)

// 実際のレーン移動の速さ。走行速度と一緒に上がる。
// 速度だけ上げると「見えているのに届かない」状態になるため、必ず連動させる。
float laneChangeSpeed() { return LANE_CHANGE_BASE * SPEED_SCALE; }
final float COLLECT_DIST      = 2.0;   // 当たり判定(前後方向)

// ---- 食べ物の配置 ----
// 原則、1列は最大2個(必ず逃げ道が1レーン残る)。例外は壁だけ。
//
// 【まばら区間の間隔は Regions.pde にある】エリアごとに違うため。
// 沖縄 0.50〜0.80秒 → 北海道 0.32〜0.50秒。
// 距離ではなく秒で持っているので、速度を上げても「忙しさ」は設計どおりに保たれる。
final float DENSE_ROW_SEC      = 0.34;  // 密集地帯の列の間隔(秒)
final float DENSE_WEAVE_CHANCE = 0.7;  // 空きレーンが隣へ動く確率
final int   DENSE_MAX_SAME_LANE = 2;   // 同じレーンが空き続けられる最大列数
final float DENSE_WOMAN_CHANCE = 0.15; // 【中盤のみ】空きレーンに女性を置く確率
// 終盤の「3個並びの壁」を置くか。
//
// 【false にした理由】
// この壁はマンジャロでしか抜けられないため、プレイヤーに薬の使用を強制していた。
// しかしこのゲームは「乱用を問題として描く」ことが目的なので、
// ゲーム側が乱用を要求しているのは矛盾している(2026-07-31 先生の指摘)。
//
// 解決として「選択を無くす」案もあったが、それだと薬がプレイヤーの手から消えて
// ただの演出になってしまう。そこで【強制だけを廃止し、選択は残す】ことにした。
// 打つか打たないかは、常にプレイヤーが決める。
//
// 壁を作るコードは消していない。true に戻せば復活する。
final boolean USE_FOOD_WALL  = false;

// 壁は、密集地帯の最後の FINAL_WALL_ROWS 列に置かれる(USE_FOOD_WALL が true のとき)。
final int   FINAL_WALL_ROWS  = 4;      // 壁の列数
final int   FINAL_WOMAN_ROWS = 3;      // 壁の直前に置く「食べ物2個+空きレーンの女性」の列数
final float FINAL_WARN_SEC   = 5;      // 終盤チャレンジの操作ヒントを何秒手前から出すか
final float COURSE_START_CLEAR = 40;
final float COURSE_END_CLEAR   = 40;
final float DENSE_EDGE_BUFFER  = 10;
final float WOMAN_LEAD_DIST    = 18;

// ---- 女性キャラと看板 ----
final float WOMAN_REVEAL_SEC = 1.2;   // 走行速度×この秒数の手前で地面から現れる
final float WOMAN_RISE_SEC   = 0.15;  // せり上がりきるまでの時間
final float WOMAN_HIDE_Y     = -3.2;  // 地面下の隠し位置
final float SIGN_LEAD_SEC    = 8;     // 壁の何秒手前に看板を立てるか(壁を復活させたときだけ使う)
final float SIGN_NEAR_SEC    = 2;

final int   COURSE_SEED = 12345;      // 同じ数字なら毎回同じコース

// ---- エリア(日本縦断のステージ) ----
// エリアの構成・地名・港の名前・難易度は Regions.pde の buildRegions() にある。
final float REGION_BANNER_SEC = 2.6;   // エリアに入ったとき地名を出しておく秒数

// エリアとエリアの間に空ける海の長さ。
// DRAW_DIST(420)より長くしてあるので、港に立っても次のエリアは見えない。
// これがないと、船に乗る前から次の土地の道が見えてしまい、
// 「同じ道の続きを走っているだけ」に見える。
final float REGION_GAP = 900;

// ---- 港 ----
// ステージのゴールは港。ゴールゲートをくぐった先に桟橋があり、船が停泊している。
final float PORT_DEPTH       = 34;   // ゴール地点から桟橋の先端までの長さ
final float PORT_NOTICE_SEC  = 4;    // 「まもなく○○港」を何秒手前から出すか
final float START_LINE_DEPTH = 2.5;  // スタートラインの帯の太さ

// ---- 描画 ----
final float DRAW_DIST = 420;   // これより遠いものは描かない(P3Dにフォグが無いので手前で消す)
final float FADE_DIST = 160;   // 遠方でこの距離ぶんかけて薄くする(唐突な出現を防ぐ)
final float CAM_HEIGHT = 5;    // カメラの高さ
final float CAM_BACK   = 11;   // 主人公の後ろへの距離
final float CAM_LOOK_Y = 1.5;  // 注視点の高さ
final float CAM_LOOK_AHEAD = 12;
final float FOV_BASE = 60;     // 画角(度)。速度が上がるほど広がる

// 主人公の右横に追従する縦向きの血糖バー(下=0 上=100)。
// 左上のメーターから目を離さずに済むよう、視線の近くにもう1つ置いている。
final float MINI_GAUGE_SIDE = 2.4;   // 主人公から右へ何units離すか
final float MINI_GAUGE_BODY_Y = 1.6; // 体のどの高さに合わせるか
final float MINI_GAUGE_W = 14;       // 設計座標での幅
final float MINI_GAUGE_H = 150;      // 設計座標での高さ

// 画面の左右とワールドのX符号の対応。
// camera() の up ベクトルを (0,-1,0) にしている都合で、+X が画面のどちら側に出るかは
// 実際に動かさないと分からない。実機で確認した結果 +1 が正しかった(-1 だと左右が逆になる)。
final float LANE_X_SIGN = 1;

// COURSE_LENGTH と speedAtZ() は Regions.pde にある。
// どちらもエリアの構成から計算されるため。

// レーン番号(0=左 1=中 2=右)を ワールドX に変換する
float laneToX(int lane) {
  return LANE_X_SIGN * (lane - 1) * LANE_WIDTH;
}
